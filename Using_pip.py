# Python3 -m                                specifies a Python module to be ran as as script
# pipi install -U <package>                 upgrade an installed package
# pip install -U pip                        upgrade pip (-U r --upgrade)
# pip list                                  show installed packages with version
# pip list -o                               show packages that aren't the newest version
# pip install <package>                     install the latest version of a package
# pip install <package>==<version#>         install a specific version of a package
# pip freeze > requirements.txt             show installed packages and versions in a "requirements format"
# pip install -r requirements.txt           install the requirements file
# pip search <keyword>                      search for packages by keyword (now depracated?)... search on https://pypi.org for a package name